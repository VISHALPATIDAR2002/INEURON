
#include <stdio.h>
int main()
{
   int a[10],sum=0,i;
   int *p;
    p=a;
   for(i=0; i<10; i++)
   {
   printf("Enter elements of array ");
   scanf("%d",&a[i]);
   }
   
   for(i=0; i<10; i++)
   {
       sum=sum+p[i];
   }
   printf("SUM = %d",sum);
    return 0;
}
