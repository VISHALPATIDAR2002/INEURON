#include <stdio.h>
int main()
{
   char a[20];
   char *p;
   int i,x;
    p=a;
  
   {
   printf("Enter a string ");
   gets(a);
   }
   x=strlen(p);
   for(i=x; i>=0; i--)
   {
       printf("%c",p[i]);
   }
   
    return 0;
}
