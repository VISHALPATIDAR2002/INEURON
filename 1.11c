#include<stdio.h>
    int main(){
        int H,M;
        printf("Enter time in hours and minutes : ");
        scanf("%d %d",&H,&M);
        printf("%d:%d\n",H,M);
        printf("%d Hour and %d minute",H,M);
        return 0;
    }
